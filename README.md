GNU tar is required, MacOS need install gnu-tar
```
$ brew install gnu-tar
```

then you need add a "gnubin" directory to your PATH from your bashrc like:
```
PATH="/usr/local/opt/gnu-tar/libexec/gnubin:$PATH"
```